---
import { cn } from '@/utils/cn';

type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---

<div
  class={cn(
    'rounded-md border border-gray-300 bg-gray-50 shadow-sm dark:border-gray-600 dark:bg-gray-800',
    className
  )}
>
  <slot />
</div>
