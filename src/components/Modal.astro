---
const {id, open} = Astro.props;
---

<button onclick="{`window.${id}.showModal()`}">{open}</button>

<dialog id="{id}">
    <slot name="main" />
    <form method="dialog">
      <slot name="close" />
    </form>
  </dialog>

  <!-- <style lang="scss">
    dialog {
      position: fixed;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%);
      &::backdrop {
        position: fixed;
        top: 0px;
        right: 0px;
        bottom: 0px;
        left: 0px;
        background: rgba(217, 212, 212, 0.487);
      }
    }
  </style> -->